import React from 'react'
import Button from 'react-bootstrap/Button'

const Formbtn = ({onClick}) => {
  return (
           
        <Button variant="primary" onClick={onClick}>Appointment</Button>
  
  )
}

export default Formbtn
